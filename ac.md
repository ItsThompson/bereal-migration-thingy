as a user I want to convert the raw data into mp4/pngs that emulate the feel of the original bereal app.
- For each raw memory data, we will process it to convert it into a single mp4/png where it goes and finds the webp or mp4 file and then uses ffmpeg to process the data?
